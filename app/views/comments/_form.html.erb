<%= form_for ([@answer, @answer.comments.build]) do |f| %>
  <div class="form-group comment-box">
    <%= f.text_area :title , class: "form-control", placeholder: "Add Comment", required: true %>
  </div>
  <%= f.submit :Submit, class: "btn btn-secondary text-uppercase" %>
<% end %>
